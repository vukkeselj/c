#include<stdio.h>

int cmp(int a, int b)
{
    return a == b;
}

ili 

#include<stdio.h>

int cmp(int a, int b)
{
    if (a == b)
        return 1;
    else
        return 0;
}

int main()
{
    if (cmp(k1, k2))
    {
        
    }
}
